class Recording
    def initialize(id, url, name)
        @id = id
        @url = url
        @name = name
    end

    def id
        @id
    end

    def url
        @url
    end

    def name
        @name
    end
end